server:
  port: 9000
spring:
  application:
    name: api-manager-auth
  redis:
    host: 192.168.6.129
    port: 6379
    database: 0
    password: minnan
  datasource:
    url: jdbc:mysql://192.168.6.129:3306/api_manager?serverTimezone=Asia/Shanghai
    username: Minnan
    password: minnan
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      max-lifetime: 600000
  cache:
    type: redis
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9030/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}:@project.version@
feign:
  hystrix:
    enabled: true

swagger:
  base-package: site.minnan.apimanager.userinterface.fascade

jwt:
  header: Authorization
  expiration: 604800
  secret: min107